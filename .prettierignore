# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# El lockfile lo gestiona npm — Prettier no debe reformatearlo
package-lock.json

# CLAUDE.md es la guía del proyecto, curada a mano — Prettier no la reformatea
CLAUDE.md

# Código generado (Prisma client) — se regenera, no lo formateamos
**/generated